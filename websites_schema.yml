type: map
mapping:
  "websites":
    type: seq
    sequence:
      - type: map
        name: Website
        mapping:
          "name":
            type: str
            required: yes
            ident: yes
            unique: yes
          "url":
            type: str
            pattern: /^(https?:\/\/)([\da-zA-Z0-9\.-]+)\.([a-zA-Z0-9\.]{2,8})([\/\w \.-]*)*\/?.*$/
            required: yes
            unique: yes
          "img":
            type: str
            pattern: /\.png$/i
          "bch":
            type: bool
            required: yes
            pattern: /(true|false)/
          "btc":
            type: bool
            pattern: /(true|false)/
          "othercrypto":
            type: bool
            pattern: /(true|false)/
          "exceptions":
            type: map
            mapping:
              "text":
                type: str
                required: yes
          "doc":
            type: str
          "twitter":
            type: str
            pattern: /(\w){1,15}$/
          "facebook":
            type: str
            pattern: /(\w){1,50}$/
          "email_address":
            type: str
            pattern: /\A([\w+\-].?)+@[a-z\d\-]+(\.[a-z]+)*\.[a-z]+\z/i
          "status":
            type: str
            desc: This should only be on a listing if its in the process of implementing BCH support but removed once it has completed. This is always a URL to the status of that effort.
            pattern: /^(https?:\/\/)([\da-zA-Z0-9\.-]+)\.([a-zA-Z0-9\.]{2,8})([\/\w \.-]*)*\/?.*$/
          "lang":
            type: str
          "region":
            type: str
            pattern: /(\w){2}$/
          "country":
            type: str
            pattern: /(\w){2}$/
          "state":
            type: str
          "city": 
            type: str
          "keywords":
            type: seq
            desc: Use this tag to add in any keywords people might use to find this result
            sequence:
              - type: str
