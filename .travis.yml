os: bash
language: ruby
addons:
  snaps:
    - name: ruby
      confinement: classic

before_install:
  - cd ./.tests/

jobs:
  include:
  - name: "Check Twitter handles"
    script: gem install twitter; bash ./twitter.sh

  - name: "Check Alexa ranking"
    script: bash ./alexa.sh
