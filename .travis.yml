os: linux
language: ruby
addons:
  snaps:
    - ruby
      confinement: classic

before_install:
  - cd ./.tests/

jobs:
  include:
  - name: "Check Twitter handles"
    script: gem install twitter; bash ./.tests/twitter.sh

  - name: "Check Alexa ranking"
    script: bash ./.tests/alexa.sh"
